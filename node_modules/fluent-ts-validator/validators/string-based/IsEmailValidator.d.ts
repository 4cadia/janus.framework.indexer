import { PropertyValidator } from "../PropertyValidator";
import { EmailOptions } from "../../shared";
export declare class IsEmailValidator implements PropertyValidator<string> {
    private options;
    constructor(options?: EmailOptions | undefined);
    isValid(input: string | undefined): boolean;
}
