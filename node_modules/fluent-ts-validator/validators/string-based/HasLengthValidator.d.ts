import { PropertyValidator } from "../PropertyValidator";
export declare class HasLengthValidator implements PropertyValidator<string> {
    private options;
    constructor(options: {
        min?: number;
        max?: number;
    });
    isValid(input: string | undefined): boolean;
}
