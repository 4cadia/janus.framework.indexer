import { PropertyValidator } from "../PropertyValidator";
import { FqdnOptions } from "../../shared";
export declare class IsFqdnValidator implements PropertyValidator<string> {
    private options;
    constructor(options?: FqdnOptions | undefined);
    isValid(input: string | undefined): boolean;
}
