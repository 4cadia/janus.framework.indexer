import { PropertyValidator } from "../PropertyValidator";
export declare class RegExValidator implements PropertyValidator<string> {
    private pattern;
    private modifiers;
    constructor(pattern: RegExp, modifiers?: string | undefined);
    isValid(input: string | undefined): boolean;
}
