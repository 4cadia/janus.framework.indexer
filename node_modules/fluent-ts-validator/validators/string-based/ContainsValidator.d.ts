import { PropertyValidator } from "../PropertyValidator";
export declare class ContainsValidator implements PropertyValidator<string> {
    private seed;
    constructor(seed: string);
    isValid(input: string | undefined): boolean;
}
