import { PropertyValidator } from "../PropertyValidator";
export declare class DoesNotContainElementValidator<TProperty> implements PropertyValidator<Iterable<TProperty>> {
    private nonSeed;
    constructor(nonSeed: TProperty);
    isValid(input: Iterable<TProperty> | undefined): boolean;
}
