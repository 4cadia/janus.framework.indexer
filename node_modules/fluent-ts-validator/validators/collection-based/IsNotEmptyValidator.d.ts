import { PropertyValidator } from "../PropertyValidator";
export declare class IsNotEmptyValidator implements PropertyValidator<Iterable<any>> {
    isValid(input: Iterable<any> | undefined): boolean;
}
