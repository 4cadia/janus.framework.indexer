import { PropertyValidator } from "../PropertyValidator";
export declare class ContainsElementValidator<TProperty> implements PropertyValidator<Iterable<TProperty>> {
    private seed;
    constructor(seed: TProperty);
    isValid(input: Iterable<TProperty> | undefined): boolean;
}
