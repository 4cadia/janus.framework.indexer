import { PropertyValidator } from "../PropertyValidator";
/**
 * Validates if given date is same as specified date.
 *
 * @export
 * @class IsSameAsValidator
 * @implements {PropertyValidator<Date>}
 */
export declare class IsSameAsValidator implements PropertyValidator<Date> {
    private date;
    constructor(date: Date);
    isValid(input: Date | undefined): boolean;
}
