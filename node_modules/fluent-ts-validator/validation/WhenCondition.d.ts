import { ValidationCondition } from "./ValidationCondition";
export declare class WhenCondition<T> implements ValidationCondition<T> {
    private expression;
    constructor(expression: (input: T) => boolean);
    shouldDoValidation(input: T): boolean;
}
