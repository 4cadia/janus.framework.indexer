import { ValidationCondition } from "./ValidationCondition";
export declare class WhenNotEmptyCondition<T> implements ValidationCondition<T> {
    private expression;
    constructor(expression: (input: T) => any);
    shouldDoValidation(input: T): boolean;
    private lambdaExprResultFor(input);
}
