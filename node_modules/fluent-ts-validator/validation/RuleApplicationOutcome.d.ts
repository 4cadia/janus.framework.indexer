import { ValidationFailure } from "../shared/ValidationFailure";
export declare class RuleApplicationOutcome {
    private validationFailure;
    private failures;
    constructor(validationFailure?: ValidationFailure | undefined);
    isSuccess(): boolean;
    isFailure(): boolean;
    addValidationFailure(failure: ValidationFailure): void;
    getValidationFailures(): ValidationFailure[];
}
